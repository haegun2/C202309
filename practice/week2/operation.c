#include <stdio.h>

int main()
{
	int x = 4;
	int y = 2;
	int z;

	z = x + y;
	printf("z = x + y = %d\n", z);
	z = x - y;
	printf("z = x - y = %d\n", z);
	z = x * y;
	printf("z = x * y = %d\n", z);
	z = x / y;
	printf("z = x / y = %d\n", z);
	//변수에 숫자를 저장 후 연산자로 계산

}